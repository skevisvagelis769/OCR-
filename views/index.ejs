<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IMG TO TEXT</title>
</head>
<body>
    <h1>IMG TO TEXT</h1>
    
        <input id="in" type="file" accept=".jpeg .png"/>
        <button id="sub" >Convert</button>
   
    <script>
        let btn=document.getElementById("sub")
        let input = document.querySelector('input[type="file"]')
        const formD = new FormData()
        formD.append('file',input.files[0])
       
        btn.addEventListener('click',()=>{
            console.log("clicked")
            console.log("This is the file info inside the html==>",input.files[0])
            console.log("This is the FORM info inside the html==>",formD)
            
            fetch('/uploads',{
                method:"POST",
                /* headers: {
                   'Accept': 'application/json', 
                    'Content-Type': 'image/png'
                }, */
                body: formD
            })
            
            
        })
        .then(response)
        /* const request = new Request("/uploads",{
            method:"POST",body:formD
        })
 */
    </script>
</body>
</html>